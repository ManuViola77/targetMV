import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  arrowBackStyle: {
    marginRight: 30,
  },

  leftIcon: {
    marginLeft: 20,
  },

  rightIcon: {
    marginRight: 20,
  },
});

export default styles;
