import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  arrowBackStyle: {
    marginLeft: 30,
  },

  leftIcon: {
    marginLeft: 20,
  },

  rightIcon: {
    marginRight: 20,
  },
});

export default styles;
