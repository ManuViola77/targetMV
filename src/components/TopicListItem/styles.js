import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  horizontalContainer: {
    flexDirection: 'row',
  },

  icon: {
    margin: 10,
  },

  name: {
    margin: 10,
  },
});

export default styles;
