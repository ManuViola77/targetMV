import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  horizontalContainer: {
    flexDirection: 'row',
  },

  icon: {
    height: 20,
    margin: 10,
    width: 20,
  },

  name: {
    margin: 10,
  },
});

export default styles;
